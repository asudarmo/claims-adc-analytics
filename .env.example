# Copy to .env and adjust if needed. .env is git-ignored — never commit real
# credentials, even for a local dev-only Postgres like this one.
POSTGRES_USER=reserving
POSTGRES_PASSWORD=reserving_dev_only
POSTGRES_DB=claims_reserving
POSTGRES_PORT=5432
