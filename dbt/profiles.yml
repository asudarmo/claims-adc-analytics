claims_reserving:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('POSTGRES_HOST', 'localhost') }}"
      user: "{{ env_var('POSTGRES_USER', 'reserving') }}"
      password: "{{ env_var('POSTGRES_PASSWORD', 'reserving_dev_only') }}"
      port: "{{ env_var('POSTGRES_PORT', '5432') | int }}"
      dbname: "{{ env_var('POSTGRES_DB', 'claims_reserving') }}"
      schema: public
      threads: 4
